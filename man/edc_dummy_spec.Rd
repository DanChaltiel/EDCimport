% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dummy.R
\name{edc_dummy_spec}
\alias{edc_dummy_spec}
\alias{edc_dummy_database}
\title{Create and use a portable dummy-data specification}
\usage{
edc_dummy_spec(db)

edc_dummy_database(spec, seed = NULL)
}
\arguments{
\item{db}{An \code{edc_database}.}

\item{spec}{A dummy specification produced by \code{edc_dummy_spec()}, including after a CSV round-trip.}

\item{seed}{Optional random seed. With the same specification and seed, the generated database is reproducible. The caller's RNG state is restored on exit.}
}
\value{
\code{edc_dummy_spec()} returns a plain \code{data.frame} that can be written to CSV and read back.

\code{edc_dummy_database()} returns an object with classes \code{edc_dummy} and \code{edc_database}.
}
\description{
\code{edc_dummy_spec()} profiles the structure of an \code{edc_database} into a flat, CSV-compatible data frame. \code{edc_dummy_database()} reconstructs an artificial database using only that specification.
}
\details{
The current implementation is deliberately minimal. It preserves dataset and column structure, basic R classes and labels, coarse univariate generation parameters, and missingness. It does not preserve clinical relationships, patient trajectories, or mixed-grain dependencies.

Real subject identifiers, character values, and calendar dates are not copied into the specification. Factor levels are treated as structural metadata and are currently preserved.

The generated database uses artificial subject identifiers and a fixed artificial extraction date. Its \code{.lookup} table is rebuilt from the dummy datasets. This functionality should not be interpreted as an anonymisation or synthetic-data method preserving clinical or statistical relationships.
}
\examples{
db = edc_example(N = 10)
spec = edc_dummy_spec(db)

file = tempfile(fileext = ".csv")
write.csv(spec, file, row.names = FALSE)
spec = read.csv(file)

dummy = edc_dummy_database(spec, seed = 42)
dummy
}
